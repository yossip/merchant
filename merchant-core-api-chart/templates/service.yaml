apiVersion: v1
kind: Service
metadata:
  name: {{ include "merchant-core-api-chart.fullname" . }}
  labels:
    {{- include "merchant-core-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "merchant-core-api-chart.selectorLabels" . | nindent 4 }}
